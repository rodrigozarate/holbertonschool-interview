#ifndef BINARY_SEARCH_ADV
#define BINARY_SEARCH_ADV

#include <stddef.h>

int recursion(int *array, int value, size_t left, size_t right);
int advanced_binary(int *array, size_t size, int value);

#endif
